// Public "Update Now" endpoint.
// Paste your Val Town HTTP endpoint URL between the quotes below, then commit.
// While it's blank, the "Update Now" button falls back to opening GitHub's
// "Run workflow" page (only repo collaborators can trigger it there).
window.ACCC_UPDATE_ENDPOINT = "";
